const initialState = {
  loading: true,
  plants: [],
  error: null,
  Alert: null,
};

export const plantreducers = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
